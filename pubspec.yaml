name: flutter_smart_button
description: A customizable smart button widget for Flutter applications, designed to handle asynchronous operations with a loading indicator, and customizable styles.
version: 0.1.0
homepage: https://github.com/yourusername/flutter_smart_button

environment:
  sdk: ">=2.12.0 <3.0.0"
  flutter: ">=1.20.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_spinkit: ^5.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
